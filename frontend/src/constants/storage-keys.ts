export const STORAGE_KEYS = {
  accessToken: 'auth.accessToken',
  authUser: 'auth.user',
  habitListCache: 'habits.list.cache',
  offlineQueueState: 'offline.queue.state',
  taskListCache: 'tasks.list.cache',
} as const;
