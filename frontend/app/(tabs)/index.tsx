import { ScreenPlaceholder } from '../../src/components/common/screen-placeholder';

export default function DashboardScreen() {
  return (
    <ScreenPlaceholder
      title="Smart Task & Habit Tracker"
      description="Dashboard scaffold is ready. Connect your first feature module here when implementation starts."
    />
  );
}
