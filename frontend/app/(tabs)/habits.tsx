import { ScreenPlaceholder } from '../../src/components/common/screen-placeholder';

export default function HabitsScreen() {
  return (
    <ScreenPlaceholder
      title="Habits"
      description="Habit tracking screens are intentionally left as placeholders for the next implementation phase."
    />
  );
}
