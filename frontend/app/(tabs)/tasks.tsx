import { ScreenPlaceholder } from '../../src/components/common/screen-placeholder';

export default function TasksScreen() {
  return (
    <ScreenPlaceholder
      title="Tasks"
      description="Task flows, forms, and list views will live here once feature work begins."
    />
  );
}
